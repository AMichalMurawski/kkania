export interface ReservationFormProps {
    darkStyle?: boolean;
}

export interface ReservationFormData {
    name: string;
    email: string;
    session: string;
    date: string;
    info: string;
};
